<%= simple_form_for @update_code, :html => { :class => 'form-horizontal' } do |f| %>

 <%= f.input :reef_tank_arduino_id , :as => :hidden, :input_html => { :value => @reef_tank.reef_tank_arduino_id   } %>
  <%= f.input :reef_tank_id %>
 
  <%= f.input :sent_email %>
  <%= f.input :message %>
  <%= f.input :priority %>
  <%= f.input :display_site_wide %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                update_codes_path, :class => 'btn' %>
  </div>
<% end %>
