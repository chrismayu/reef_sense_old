<%= simple_form_for @user_livestock, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :reef_tank_id %>
  <%= f.input :scientific_name %>
  <%= f.input :description %>
  <%= f.input :image %>
  <%= f.input :livestock_type %>
  <%= f.input :common_name %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                user_livestocks_path, :class => 'btn' %>
  </div>
<% end %>
